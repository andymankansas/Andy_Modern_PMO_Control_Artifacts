# Workbook Tools

This folder will contain deterministic scripts for workbook inspection, proposal validation, approved updates, and post-write verification.

Workbook writes must consume a schema-valid approval manifest. Agent-generated ad hoc workbook mutation commands are not part of the supported workflow.
