# Configuration

This folder will contain the distributable example configuration and JSON schemas.

Never commit a recipient's runtime `raid-config.json`. It may contain local paths, account identifiers, project keywords, and source locations.
