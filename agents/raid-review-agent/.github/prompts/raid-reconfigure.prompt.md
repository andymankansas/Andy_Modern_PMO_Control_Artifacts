---
name: RAID Reconfigure
description: "Change selected RAID Review Agent settings while preserving all other configuration."
agent: RAID Review Agent
argument-hint: "Describe settings to add, remove, or change."
---

Load `config/raid-config.json` and show a concise current summary.

Let me change only the settings I name while preserving all others. Validate changed paths, workbook mappings, and knowledge-source settings before saving. Revalidate the complete configuration afterward.

Do not review, approve, or modify the RAID workbook.