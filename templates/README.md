# Workbook Templates

The sanitized `Workback_RAID_Template.xlsx` will be included here before the first release.

The packaged template is a source asset and must never be edited in place. Setup copies it to the recipient's selected destination before configuration or use.
